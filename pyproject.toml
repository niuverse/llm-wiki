[project]
name = "llm-wiki-tools"
version = "0.1.0"
description = "Local helper tools for the personal LLM wiki."
requires-python = ">=3.12,<3.13"
dependencies = [
  "markitdown-ocr[llm]>=0.1.0",
  "markitdown[all]>=0.1.5",
  "openai>=1.0.0",
]

[tool.uv]
package = false
